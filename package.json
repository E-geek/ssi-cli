{
  "name": "ssi-cli",
  "version": "0.0.1",
  "description": "build files wih ssi directives by cli",
  "main": "bin/ssi.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "coffee --no-header -c bin lib/ssi.coffee"
  },
  "bin": {
    "ssi": "./bin/ssi"
  },
  "keywords": [
    "ssi",
    "cli"
  ],
  "author": "E-geek",
  "license": "MIT",
  "dependencies": {
    "cson": "^3.0.2",
    "node-ssi": "^0.3.1",
    "optimist": "=0.6.1"
  },
  "devDependencies": {
    "coffee-script": "=1.9.3"
  }
}
